apiVersion: apps/v1
kind: Deployment
metadata:
  name: cbs-service
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cbs-service
  template:
    metadata:
      labels:
        app: cbs-service
    spec:
      imagePullSecrets:
        - name: dockerhub-registry-secret
      containers:
        - name: cbs-service
          image: docker.io/sudhavuppalapati/cbs-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8097
          envFrom:
            - configMapRef:
                name: cbs-service-config
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:TieredStopAtLevel=1 -Xms128m -Xmx400m"
          startupProbe:
            httpGet:
              path: /cbs/actuator/health
              port: 8097
            periodSeconds: 5
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /cbs/actuator/health
              port: 8097
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /cbs/actuator/health
              port: 8097
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 4
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
