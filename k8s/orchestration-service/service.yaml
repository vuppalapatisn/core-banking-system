apiVersion: v1
kind: Service
metadata:
  name: orchestration-service
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: orchestration-service
  ports:
    - name: http
      port: 8096
      targetPort: 8096
