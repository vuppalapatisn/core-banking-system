apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-service
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-service
  template:
    metadata:
      labels:
        app: payments-service
    spec:
      imagePullSecrets:
        - name: dockerhub-registry-secret
      containers:
        - name: payments-service
          image: docker.io/sudhavuppalapati/payments-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8100
          envFrom:
            - configMapRef:
                name: payments-service-config
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:TieredStopAtLevel=1 -Xms128m -Xmx400m"
          startupProbe:
            httpGet:
              path: /payments/actuator/health
              port: 8100
            periodSeconds: 5
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /payments/actuator/health
              port: 8100
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /payments/actuator/health
              port: 8100
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 4
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
