apiVersion: v1
kind: Service
metadata:
  name: lms-service
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: lms-service
  ports:
    - name: http
      port: 8099
      targetPort: 8099
